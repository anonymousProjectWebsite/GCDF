# Map Generator Package

This package publishes the fixed example point cloud used by the REMANI reproduction workspace. The selected map folder is configured in:

```text
../../remani_planner/plan_manage/launch/run_in_sim.launch
```
